@model IEnumerable<HouseOfShows.WebMVC.Models.TipoIngresso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>TipoIngresso</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @item.TipoIngresso1
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoIngresso1">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoIngresso1">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoIngresso1">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
